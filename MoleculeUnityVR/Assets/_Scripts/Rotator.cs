using UnityEngine;
using System.Collections;
 
public class Rotator : MonoBehaviour {

    //public LoadCmlData cmlDataLoader;
     
    //string name = GetComponent<LoadCmlData>().MoleculeName;

    void Update(){

        //GameObject molecule = GameObject.Find(name);

        //molecule.transform.Rotate(0,20*Time.deltaTime,0);

    }
     
}